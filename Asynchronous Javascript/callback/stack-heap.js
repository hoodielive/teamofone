/** 
 * Describe what a stack is. An ordered data structure. Keeps track of function invocations. 
 * Part of the javascript runtime (you don't access it directly).
 * Describe a stack frame.
 * Describe a heap.
*/

function multiply(x, y) 
{
  return x * y; 
}

// stack main
let res = multiply(3, 5); 

console.log(res);
