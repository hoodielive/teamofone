function doAsyncTask(cb) {
  cb();

}
// work on this later
setTimeout(function(doAsyncTask_ => console.log(message), 3000); 

  //doAsyncTask(_ => console.log(message));

let message = "callback called";
